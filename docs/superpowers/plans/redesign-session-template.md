# Planevo V3 Redesign — Chat Session Init Template

> **Historical template.** Master plan: [`2026-06-15-planevo-v3-comprehensive-redesign-plan.md`](2026-06-15-planevo-v3-comprehensive-redesign-plan.md). Product SSOT: [`apps/web/STRATEGY.md`](../../apps/web/STRATEGY.md).

Copy and paste the text below into the first prompt of any new chat session to onboard the agent onto the current phase of the redesign.

***

```markdown
We are executing the Planevo V3 Comprehensive Ecosystem Redesign. To prevent context drift and stay highly focused, you are restricted to working ONLY on the phase specified below.

### Core Instructions
1. READ the master plan document first:
   /Users/jabbo/M1plan/planevo/docs/superpowers/plans/2026-06-15-planevo-v3-comprehensive-redesign-plan.md
2. REVIEW the reference images in:
   /Users/jabbo/M1plan/planevo/docs/superpowers/plans/reference-images/
   Specifically examine the reference image(s) corresponding to the target platform/surface of this phase.
3. DO NOT stray into future phases or begin work on unrelated parts of the codebase. Focus entirely on completing the checklist items for the active phase.
4. Follow the non-negotiable Security and Accessibility mandates specified in Section 8 of the master plan.
5. Apply the specific skill rules (e.g., /frontend-developer, /frontend-design, /frontend-security-coder, /ui-ux-pro-max, /ios-developer, /modern-web-guidance, /top-web-vulnerabilities, /canvas-design) relevant to this phase.
6. Track progress by creating/updating the `task.md` artifact in the brain directory.

---

### Active Phase & Goal
**Active Phase:** Phase 1: Design System Foundation (Week 1-2)
**Goal:** Build the unified design token file and core components (DayPickerStrip, FilterPills, TaskCard, StatTile, BrunoCard, GlassSheet) with full responsiveness and visual QA.
**Current Task:** Let's start on Phase One. Specifically, let's create the unified design token files (`tokens.css` for web, `tokens.ts` for mobile).

2. REVIEW the reference images in:
   /Users/jabbo/M1plan/planevo/docs/superpowers/plans/reference-images/
   Specifically examine the reference image(s) corresponding to the target platform/surface of this phase.
3. DO NOT stray into future phases or begin work on unrelated parts of the codebase. Focus entirely on completing the checklist items for the active phase.
4. Follow the non-negotiable Security and Accessibility mandates specified in Section 8 of the master plan.
5. Apply the specific skill rules (e.g., /frontend-developer, /frontend-design, /frontend-security-coder, /ui-ux-pro-max, /ios-developer, /modern-web-guidance, /top-web-vulnerabilities, /canvas-design) relevant to this phase.
6. Track progress by creating/updating the `task.md` artifact in the brain directory.

---

### Active Phase & Goal
[USER: Replace this block with your active phase instructions, e.g.:]
**Active Phase:** Phase 1: Design System Foundation (Week 1-2)
**Goal:** Build the unified design token file and core components (DayPickerStrip, FilterPills, TaskCard, StatTile, BrunoCard, GlassSheet) with full responsiveness and visual QA.
**Current Task:** Let's start on building the unified design token files (`tokens.css` for web, `tokens.ts` for mobile).
```
